# 🏦 Bank Marketing Prediction

**ML + Cloud Coursework — Predicting Term Deposit Subscription**

> Predict whether a Portuguese bank client will subscribe to a term deposit based on telemarketing campaign data.

---

## 📁 Project Structure

```
bank_marketing_prediction/
├── .github/
│   └── workflows/
│       └── ci-cd.yml              # GitHub Actions CI/CD pipeline
├── data/                          # Dataset (generated by download_data.py)
├── model/                         # Saved model pipeline + feature info
│   ├── lr_deployment_pipeline.joblib
│   └── feature_info.json
├── figures/                       # Generated plots from notebook
├── templates/                     # Flask HTML templates
│   ├── index.html                 # Prediction form (7 input fields)
│   └── result.html                # Prediction result page
├── docs/                          # Architecture & CI/CD diagrams
│   └── REPORT.md                  # Full coursework report
├── bank_marketing_prediction.ipynb  # Main notebook (Tasks A-C)
├── app.py                         # Flask web application
├── download_data.py               # Dataset download utility
├── Dockerfile                     # Container build file
├── docker-compose.yml             # Local testing
├── requirements.txt               # Python dependencies
└── README.md                      # This file
```

## 🚀 Quick Start

### 1. Download the Dataset
```bash
python download_data.py
```

### 2. Run the Notebook
```bash
pip install -r requirements.txt
jupyter lab bank_marketing_prediction.ipynb
```
Run all cells to train models, generate figures, and export the deployment model.

### 3. Test Locally
```bash
# Option A: Flask dev server
python app.py

# Option B: Docker
docker-compose up --build
```
Open http://localhost:5000

### 4. Deploy to AWS EC2
See the [Deployment Guide](docs/REPORT.md#task-d--deployment) in the report.

---

## 📊 Models

| Model | Dataset | F1 | ROC-AUC |
|-------|---------|-----|---------|
| Logistic Regression | Realistic (no duration) | TBD | TBD |
| SVM (RBF) | Realistic (no duration) | TBD | TBD |
| **LR (Deployed)** | **7 form fields** | **TBD** | **TBD** |

> Metrics are populated after running the notebook.

## 🔧 Deployment Features (Form Inputs)
1. `age` — Client age (numeric)
2. `job` — Type of job (categorical)
3. `marital` — Marital status (categorical)
4. `education` — Education level (categorical)
5. `default` — Has credit in default? (yes/no/unknown)
6. `housing` — Has housing loan? (yes/no/unknown)
7. `loan` — Has personal loan? (yes/no/unknown)